#include <stdio.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 * Return: 0
 */

int main(void)
{
har c = 'a';

while (c <= 'z')
{
putchar(c);
c++;
}
putchar('\n');

return (0);
}
